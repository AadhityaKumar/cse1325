# Starter pipeline

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml


trigger:
- main

stages:
  - stage: build
    jobs:
    - job: run_build
      pool:
        vmImage: 'windows-latest'
      steps:
      - script: echo Hello, world!
        displayName: 'Run a one-line script'

      - script: |
          echo Add other tasks to build, test, and deploy your project.
          echo See https://aka.ms/yaml
          make
          shapes.exe
        displayName: 'Run a multi-line script'
        workingDirectory: '$(System.DefaultWorkingDirectory)/P09/full_credit'

      - task: CopyFiles@2
        inputs:
          sourceFolder: '$(System.DefaultWorkingDirectory)/P09/full_credit'
          contents: |      
            **/*.*
          targetFolder: '$(Build.ArtifactStagingDirectory)'
      - task: PublishBuildArtifacts@1
        inputs:
          pathToPublish: '$(Build.ArtifactStagingDirectory)'
          artifactName: drop

  - stage: deploy
    jobs:
    - deployment: DeployTest
      displayName: 'Deploy to Development Environment
      pool:
        vmImage: windows-latest'
      environment:
        name: 'Aadhitya_Deployment_Server'
        #resourceName: LAPTOP-I73C2NHS
        resourceType: virtualMachine
      strategy:
        runOnce:
          deploy:
            steps:
            - download: none

            - task: DownloadPipelineArtifact@2
              inputs:
                buildType: 'current'
                artifactName: drop
                targetPath: 'C:\AzureDevops\DeploymentTest\cpp'
                patterns: '**/*.*'
